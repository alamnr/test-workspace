<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>info.ejava.examples.build</groupId>
    <artifactId>ejava-build-parent</artifactId>
    <version>6.1.1-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>EJava Build::Parent</name>
    <description>
        This project defines build constructs for ejava-springboot examples,
        solutions, and projects.
    </description>

    <properties>
        <ejava.version>6.1.1-SNAPSHOT</ejava.version>
        <springboot.version>3.5.5</springboot.version>
        <maven.min.version>3.6.3</maven.min.version>

        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.target.version>21</java.target.version>
        <spring-boot.attach>false</spring-boot.attach>
        <spring-boot.classifier>bootexec</spring-boot.classifier>

        <!-- maven plugins: http://maven.apache.org/plugins/index.html -->
        <!-- core plugins -->
        <maven-clean-plugin.version>3.4.1</maven-clean-plugin.version>
        <maven-compiler-plugin.version>3.14.0</maven-compiler-plugin.version>
        <maven-dependency-plugin.version>3.8.1</maven-dependency-plugin.version>
        <maven-deploy-plugin.version>3.1.4</maven-deploy-plugin.version>
        <maven-enforcer-plugin.version>3.5.0</maven-enforcer-plugin.version>
        <maven-failsafe-plugin.version>3.5.2</maven-failsafe-plugin.version>
        <maven-help-plugin.version>3.5.1</maven-help-plugin.version>
        <maven-install-plugin.version>3.1.4</maven-install-plugin.version>
        <maven-jar-plugin.version>3.4.2</maven-jar-plugin.version>
        <maven-release-plugin.version>3.1.1</maven-release-plugin.version>
        <maven-resources-plugin.version>3.3.1</maven-resources-plugin.version>
        <maven-site-plugin.version>3.21.0</maven-site-plugin.version>
        <maven-source-plugin.version>3.3.1</maven-source-plugin.version>
        <maven-surefire-plugin.version>3.5.2</maven-surefire-plugin.version>
        <maven-war-plugin.version>3.4.0</maven-war-plugin.version>
        <maven-verifier-plugin.version>1.1</maven-verifier-plugin.version>

        <!-- tools plugins -->
        <asciidoctor-maven-plugin.version>3.1.1</asciidoctor-maven-plugin.version>
        <assertj-assertions-generator.version>2.2.0</assertj-assertions-generator.version>
        <build-helper-maven-plugin.version>3.6.0</build-helper-maven-plugin.version>
        <docker-compose-maven-plugin.version>1.0.0</docker-compose-maven-plugin.version>
        <dockerfile-maven-plugin.version>1.4.13</dockerfile-maven-plugin.version>
        <docker-maven-plugin.version>0.44.0</docker-maven-plugin.version>
        <flatten-maven-plugin.version>1.7.0</flatten-maven-plugin.version>
        <gmavenplus-plugin.version>4.1.1</gmavenplus-plugin.version>
        <heroku-maven-plugin.version>3.0.4</heroku-maven-plugin.version>
        <jacoco-maven-plugin.version>0.8.12</jacoco-maven-plugin.version>
        <maven-antrun-plugin.version>3.1.0</maven-antrun-plugin.version>
        <maven-javadoc-plugin.version>3.11.2</maven-javadoc-plugin.version>
        <maven-surefire-report-plugin.version>3.5.2</maven-surefire-report-plugin.version>
        <openapi-generator-maven-plugin.version>7.8.0</openapi-generator-maven-plugin.version>

        <!-- other plugins -->
    </properties>

    <repositories>
        <repository>
            <id>ejava-nexus-releases</id>
            <url>https://pika.cs.ep.jhu.edu/nexus/repository/ejava-releases</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>false</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>ejava-nexus-snapshots</id>
            <url>https://pika.cs.ep.jhu.edu/nexus/repository/ejava-snapshots</url>
            <releases>
                <enabled>false</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
                <updatePolicy>daily</updatePolicy>
            </snapshots>
        </repository>
    </repositories>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-antrun-plugin</artifactId>
                    <version>${maven-antrun-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven-clean-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven-dependency-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven-compiler-plugin.version}</version>
                    <configuration>
                        <release>${java.target.version}</release>
                        <!-- add metadata to method parameters -->
                        <parameters>true</parameters>
                        <!-- must be explicitly set with newer JDKs to work with lombok -->
                        <proc>full</proc>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <version>${maven-deploy-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven-enforcer-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>enforce-versions</id>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                            <configuration>
                                <rules>
                                    <requiredMavenVersion>
                                        <version>${maven.min.version}</version>
                                    </requiredMavenVersion>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-failsafe-plugin</artifactId>
                    <version>${maven-failsafe-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-help-plugin</artifactId>
                    <version>${maven-help-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <version>${maven-install-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-jar-plugin</artifactId>
                    <version>${maven-jar-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-release-plugin</artifactId>
                    <version>${maven-release-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources-plugin.version}</version>
                    <configuration>
                        <escapeString>\</escapeString>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-source-plugin</artifactId>
                    <version>${maven-source-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>attach-sources</id>
                            <phase>verify</phase>
                            <goals>
                                <goal>jar-no-fork</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-report-plugin</artifactId>
                    <version>${maven-surefire-report-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-site-plugin</artifactId>
                    <version>${maven-site-plugin.version}</version>
                    <configuration>
                        <skip>true</skip>
                        <skipDeploy>true</skipDeploy>
                    </configuration>
                    <dependencies>
                        <dependency>
                            <groupId>org.asciidoctor</groupId>
                            <artifactId>asciidoctor-converter-doxia-module</artifactId>
                            <version>${asciidoctor-maven-plugin.version}</version>
                        </dependency>
                    </dependencies>

                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${maven-surefire-plugin.version}</version>
                    <configuration>
                        <skip>${itOnly}</skip>
                    </configuration>
                </plugin>
                <plugin>
                    <artifactId>maven-javadoc-plugin</artifactId>
                    <groupId>org.apache.maven.plugins</groupId>
                    <version>${maven-javadoc-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-war-plugin</artifactId>
                    <version>${maven-war-plugin.version}</version>
                    <configuration>
                        <failOnMissingWebXml>false</failOnMissingWebXml>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.assertj</groupId>
                    <artifactId>assertj-assertions-generator-maven-plugin</artifactId>
                    <version>${assertj-assertions-generator.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>generate-assertions</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <!--
                        <templates>
                            <templatesDirectory>src/test/resources/templates/</templatesDirectory>
                            <bddEntryPointAssertionClass>ejava_bdd_assertions_entry_point_class_template.txt</bddEntryPointAssertionClass>
                        </templates>
                        -->
                    </configuration>
                </plugin>

                <plugin>
                    <groupId>io.brachu</groupId>
                    <artifactId>docker-compose-maven-plugin</artifactId>
                    <version>${docker-compose-maven-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>build-helper-maven-plugin</artifactId>
                    <version>${build-helper-maven-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>io.fabric8</groupId>
                    <artifactId>docker-maven-plugin</artifactId>
                    <version>${docker-maven-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>com.spotify</groupId>
                    <artifactId>dockerfile-maven-plugin</artifactId>
                    <version>${dockerfile-maven-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>flatten-maven-plugin</artifactId>
                    <version>${flatten-maven-plugin.version}</version>
                    <configuration>
                        <flattenMode>oss</flattenMode>
                    </configuration>
                    <executions>
                        <execution>
                            <id>flatten</id>
                            <phase>process-resources</phase>
                            <goals>
                                <goal>flatten</goal>
                            </goals>
                        </execution>
                        <execution>
                            <id>flatten.clean</id>
                            <phase>clean</phase>
                            <goals>
                                <goal>clean</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>

                <plugin>
                    <groupId>org.jacoco</groupId>
                    <artifactId>jacoco-maven-plugin</artifactId>
                    <version>${jacoco-maven-plugin.version}</version>
                </plugin>

                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${springboot.version}</version>
                    <configuration>
                        <attach>${spring-boot.attach}</attach>
                        <classifier>${spring-boot.classifier}</classifier>
                    </configuration>
                    <executions>
                      <execution>
                        <id>build-app</id>
                        <phase>package</phase>
                        <goals>
                          <goal>repackage</goal>
                        </goals>
                      </execution>
                    </executions>
                </plugin>

                <!-- swagger/openui client generator -->
                <plugin>
                    <groupId>org.openapitools</groupId>
                    <artifactId>openapi-generator-maven-plugin</artifactId>
                    <version>${openapi-generator-maven-plugin.version}</version>
                    <executions>
                        <execution>
                            <goals>
                                <goal>generate</goal>
                            </goals>
                            <configuration>
                                <generatorName>java</generatorName>
                                <configOptions>
                                    <sourceFolder>src/gen/java/main</sourceFolder>
                                </configOptions>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

            </plugins>
        </pluginManagement>
    </build>

    <profiles>
        <profile>
            <!-- This profile will add mockito-core as an approved javaagent to eliminate the following warning:

                -javaagent:${org.mockito:mockito-core:jar}
                Mockito is currently self-attaching to enable the inline-mock-maker. This will no longer work
                in future releases of the JDK. Please add Mockito as an agent to your build ...
                https://github.com/mockito/mockito/issues/3037#issuecomment-2379470509

                -Xshare:off
                Java HotSpot(TM) 64-Bit Server VM warning: Sharing is only supported for boot loader
                classes because bootstrap classpath has been appended
            -->
            <id>test</id>
            <activation>
                <file>
                    <exists>${basedir}/src/test/java</exists>
                </file>
            </activation>
            <properties>
                <!-- alternate approach without dependency plugin
                <mockito.version>5.14.2</mockito.version>
                <mockito.junit.argLine>-javaagent:${settings.localRepository}/org/mockito/mockito-core/${mockito.version}/mockito-core-${mockito.version}.jar</mockito.junit.argLine>
                -->
                <argLine/> <!-- default empty value -->
                <mockito.junit.argLine>-javaagent:${org.mockito:mockito-core:jar} -Xshare:off</mockito.junit.argLine>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <executions>
                            <execution>
                                <goals> <!-- added to get mockito definition for surefire agent -->
                                    <goal>properties</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <version>${maven-surefire-plugin.version}</version>
                        <configuration>
                            <argLine>@{argLine} ${mockito.junit.argLine}</argLine>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>it</id>
            <activation>
                <file>
                    <exists>${basedir}/src/test/resources/application-it.properties</exists>
                </file>
                <property>
                    <name>!skipITs</name>
                </property>
            </activation>
            <build>
                <pluginManagement>
                    <plugins>
                        <!-- assigns a random port# to property server.http.port -->
                        <plugin>
                            <groupId>org.codehaus.mojo</groupId>
                            <artifactId>build-helper-maven-plugin</artifactId>
                            <executions>
                                <execution>
                                    <id>reserve-network-port</id>
                                    <goals>
                                        <goal>reserve-network-port</goal>
                                    </goals>
                                    <phase>process-resources</phase>
                                    <configuration>
                                        <portNames>
                                            <portName>server.http.port</portName>
                                            <portName>server.jmx.port</portName>
                                        </portNames>
                                    </configuration>
                                </execution>
                            </executions>
                        </plugin>

                        <plugin>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-maven-plugin</artifactId>
                            <version>${springboot.version}</version>
                            <executions>
                                <execution>
                                    <id>pre-integration-test</id>
                                    <goals>
                                        <goal>start</goal>
                                    </goals>
                                    <configuration>
                                        <skip>${spring-boot.run.skip}</skip>
                                        <jmxPort>${server.jmx.port}</jmxPort> <!-- use unique port -->
                                        <arguments>
                                            <argument>--server.port=${server.http.port}</argument>
                                        </arguments>
                                    </configuration>
                                </execution>
                                <execution>
                                    <id>post-integration-test</id>
                                    <goals>
                                        <goal>stop</goal>
                                    </goals>
                                    <configuration>
                                        <skip>${spring-boot.stop.skip}</skip>
                                    </configuration>
                                </execution>
                            </executions>
                        </plugin>

                        <plugin>
                            <groupId>org.apache.maven.plugins</groupId>
                            <artifactId>maven-failsafe-plugin</artifactId>
                            <executions>
                                <execution>
                                    <id>integration-test</id>
                                    <goals>
                                        <goal>integration-test</goal>
                                    </goals>
                                    <configuration>
                                        <systemPropertyVariables>
                                            <it.server.port>${server.http.port}</it.server.port>
                                        </systemPropertyVariables>
                                    </configuration>
                                </execution>
                                <execution>
                                    <id>verify</id>
                                    <goals>
                                        <goal>verify</goal>
                                    </goals>
                                </execution>
                            </executions>
                        </plugin>
                    </plugins>
                </pluginManagement>

                <plugins> <!-- activate the configuration -->
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                    </plugin>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!--
            The following is used to derive the hostname of where Docker images are running
            that were launched by the tests.

            Builds performed within the native environment with a local Dockerhost, will use
            localhost or the value of the expanded ${docker.host.address} provided by
            fabric8io:docker-maven-plugin.

            CI/CD builds run within Docker and we must use a wormhole technique. This involes
            mapping a hostname (host.docker.internal) to the magic "host-gateway" address in the
            launch of the Docker image. This is in place by the root-level docker-compose.yml file.

            We are making use of the TESTCONTAINERS_HOST_OVERRIDE environment variable to
            know we are running within Docker and must use the wormhole technique.
        -->
        <profile> <!-- build is running within Docker-based CI/CD via root level docker-compose.yml -->
            <id>wormhole-build</id>
            <activation>
                <property>
                    <name>env.TESTCONTAINERS_HOST_OVERRIDE</name>
                </property>
            </activation>
            <properties> <!-- this hostname is mapped to "host-gateway", used by testcontainers, but generically usable -->
                <ejava-parent.docker.hostname>${env.TESTCONTAINERS_HOST_OVERRIDE}</ejava-parent.docker.hostname>
            </properties>
        </profile>
        <profile> <!-- build is running outside of Docker/root-level docker-compose.yml -->
            <id>native-build</id>
            <activation>
                <property>
                    <name>!env.TESTCONTAINERS_HOST_OVERRIDE</name>
                </property>
            </activation>
            <properties> <!-- localhost outside of Docker CI/CD -->
                <ejava-parent.docker.hostname>localhost</ejava-parent.docker.hostname>
            </properties>
        </profile>

        <!--
            This profile will turn off spring-boot-maven-plugin start/stop of the Spring Boot
            executable JAR and will enable basic settings to build, start, and stop a single
            image defined be a specific Dockerfile as part of a Maven integration test.
        -->
        <profile>
            <id>it-docker</id>
            <activation>
                <file>
                    <exists>${basedir}/src/test/properties/it-docker.activate</exists>
                </file>
            </activation>
            <properties>
                <!-- turn off launch of unwrapped Spring Boot server during integration-test-->
                <spring-boot.run.skip>true</spring-boot.run.skip>
                <spring-boot.stop.skip>true</spring-boot.stop.skip>
                <!-- define some overridable defaults for this plugin -->
                <ejava.docker.imageName>${project.artifactId}</ejava.docker.imageName>
                <ejava.docker.imageTag>${project.version}</ejava.docker.imageTag>
                <ejava.docker.dockerFile>src/main/docker/Dockerfile</ejava.docker.dockerFile>
                <ejava.docker.wait.url>http://${ejava-parent.docker.hostname}:${server.http.port}/actuator/health</ejava.docker.wait.url>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <configuration combine.self="override">
                            <verbose>true</verbose>
                            <containerNamePattern>%a-%t</containerNamePattern>
                            <images>
                                <image>
                                    <external>
                                        <type>properties</type>
                                        <prefix>ejava.docker</prefix>
                                        <mode>override</mode>
                                    </external>
                                    <name>${ejava.docker.imageName}:${ejava.docker.imageTag}</name>
                                    <build>
                                        <dockerFile>${ejava.docker.dockerFile}</dockerFile>
                                    </build>
                                    <run>
                                        <ports> <!-- map external test port to internal server 8080 port -->
                                            <port>${server.http.port}:8080</port>
                                        </ports>
                                        <wait>
                                            <url>${ejava.docker.wait.url}</url>
                                            <time>20000</time>
                                        </wait>
                                        <extraHosts> <!-- mapping docker.host.internal to Docker host running images -->
                                            <host>docker.host.internal:host-gateway</host>
                                        </extraHosts>
                                    </run>
                                </image>
                            </images>
                        </configuration>

                        <executions>
                            <execution>
                                <id>build-image</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>start-container</id>
                                <goals>
                                    <goal>start</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>stop-container</id>
                                <goals>
                                    <goal>stop</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-failsafe-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>integration-test</id>
                                <goals>
                                    <goal>integration-test</goal>
                                </goals>
                                <configuration>
                                    <systemPropertyVariables combine.children="append">
                                        <it.server.host>${ejava-parent.docker.hostname}</it.server.host>
                                    </systemPropertyVariables>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                </plugins>
            </build>
        </profile>

        <profile>
            <id>bdd</id>
            <build>
                <pluginManagement>
                    <plugins>
                        <plugin>
                            <groupId>org.apache.maven.plugins</groupId>
                            <artifactId>maven-surefire-plugin</artifactId>
                            <configuration>
                                <statelessTestsetReporter implementation="org.apache.maven.plugin.surefire.extensions.junit5.JUnit5Xml30StatelessReporter">
                                    <disable>false</disable>
                                    <version>3.0</version>
                                    <usePhrasedFileName>false</usePhrasedFileName>
                                    <usePhrasedTestSuiteClassName>true</usePhrasedTestSuiteClassName>
                                    <usePhrasedTestCaseClassName>true</usePhrasedTestCaseClassName>
                                    <usePhrasedTestCaseMethodName>true</usePhrasedTestCaseMethodName>
                                </statelessTestsetReporter>
                                <consoleOutputReporter implementation="org.apache.maven.plugin.surefire.extensions.junit5.JUnit5ConsoleOutputReporter">
                                    <disable>false</disable>
                                    <encoding>UTF-8</encoding>
                                    <usePhrasedFileName>false</usePhrasedFileName>
                                </consoleOutputReporter>
                                <statelessTestsetInfoReporter implementation="org.apache.maven.plugin.surefire.extensions.junit5.JUnit5StatelessTestsetInfoReporter">
                                    <disable>false</disable>
                                    <usePhrasedFileName>false</usePhrasedFileName>
                                    <usePhrasedClassNameInRunning>true</usePhrasedClassNameInRunning>
                                    <usePhrasedClassNameInTestCaseSummary>true</usePhrasedClassNameInTestCaseSummary>
                                </statelessTestsetInfoReporter>
                            </configuration>
                        </plugin>
                    </plugins>
                </pluginManagement>
            </build>
        </profile>

        <!-- enable spock testing -->
        <profile>
            <id>spock</id>
            <activation>
                <file>
                    <exists>${basedir}/src/test/groovy</exists>
                </file>
            </activation>

            <dependencies>
                <dependency>
                    <groupId>org.apache.groovy</groupId>
                    <artifactId>groovy</artifactId>
                    <scope>provided</scope>
                </dependency>

                <!-- Mandatory dependencies for using Spock -->
                <dependency>
                    <groupId>org.spockframework</groupId>
                    <artifactId>spock-core</artifactId>
                    <scope>test</scope>
                </dependency>

                <dependency>
                    <groupId>org.spockframework</groupId>
                    <artifactId>spock-spring</artifactId>
                    <scope>test</scope>
                </dependency>

                <!-- enables mocking of classes (in addition to interfaces)
                <dependency>
                  <groupId>net.bytebuddy</groupId>
                  <artifactId>byte-buddy</artifactId>
                  <version>1.11.0</version>
                  <scope>test</scope>
                </dependency>
                -->
                <!-- enables mocking of classes without default constructor (together with CGLIB)
                <dependency>
                  <groupId>org.objenesis</groupId>
                  <artifactId>objenesis</artifactId>
                  <version>3.2</version>
                  <scope>test</scope>
                </dependency>
                -->
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <!-- used to compile Groovy code -->
                        <groupId>org.codehaus.gmavenplus</groupId>
                        <artifactId>gmavenplus-plugin</artifactId>
                        <version>${gmavenplus-plugin.version}</version>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>compile</goal>
                                    <goal>compileTests</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                    <!-- required if names of spec classes don't match default Surefire patterns (`*Test` etc.) -->
                    <plugin>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <useFile>false</useFile>
                            <includes>
                                <include>**/*Test.java</include>
                                <include>**/*Spec.java</include>
                            </includes>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>


        <!--  tell Eclipse what to do with some of the plugins -->
        <profile>
            <id>m2e</id>
            <activation>
                <property>
                    <name>m2e.version</name>
                </property>
            </activation>
            <build>
                <pluginManagement>
                    <plugins>
                        <plugin>
                            <groupId>org.eclipse.m2e</groupId>
                            <artifactId>lifecycle-mapping</artifactId>
                            <version>1.0.0</version>
                            <configuration>
                                <lifecycleMappingMetadata>
                                    <pluginExecutions>


                                    </pluginExecutions>
                                </lifecycleMappingMetadata>
                            </configuration>
                        </plugin>

                    </plugins>
                </pluginManagement>
            </build>
        </profile>
    </profiles>

    <distributionManagement>
        <repository>
            <id>ejava.release.repo</id>
            <url>${repo.release.deploy.url}</url>
        </repository>
        <snapshotRepository>
            <id>ejava.snapshot.repo</id>
            <url>${repo.snapshot.deploy.url}</url>
        </snapshotRepository>
    </distributionManagement>
</project>
